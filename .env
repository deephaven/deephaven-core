# Note: if this file is changed, be certain to change all `.env`
#       files throughout this repository. Use `find . -name "*.env"`
#       to locate them all.

COMPOSE_PROJECT_NAME=core
REPO=ghcr.io/
TAG=main
PORT=10000
DEEPHAVEN_CONSOLE_TYPE=python
DEEPHAVEN_APPLICATION_DIR=/data/app.d
