apply from: "$rootDir/gradle/web-client.gradle"

// This module is dedicated to the RPC interfaces used to talk to the OpenAPI.
// It should be considered the culmination of all shared code, and depend
// on everything necessary to "speak OpenAPI".

dependencies {
    api project(':open-api-shared-ide')

}
