plugins {
    id 'java-library'
    id 'io.deephaven.project.register'
}

description 'Engine Validation: Interfaces for formula validation'

dependencies {
    api project(':engine-table')
    testImplementation project(':server-test-utils')
}