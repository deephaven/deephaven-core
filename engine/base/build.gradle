import io.deephaven.tools.Tools

configurations {
}

sourceSets {
    main {
        java {
            srcDir 'java'
        }
    }
    test {
        java {
            srcDir 'test'
        }
    }
}

dependencies {
    compile project(':engine-rowset'),
            project(':engine-api')
}

spotless {
    java {
        targetExclude(
        )
    }
}
