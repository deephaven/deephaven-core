import io.deephaven.tools.Tools

configurations {
}

sourceSets {
    main {
        java {
            srcDir 'java'
        }
    }
}

dependencies {
    compile project(':engine-tuple'),
            project(':engine-api'),
            project(':engine-base')
}

spotless {
    java {
        targetExclude(
                '**/generated/**',
        )
    }
}
