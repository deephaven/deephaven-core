import io.deephaven.tools.Tools

configurations {
    testCompile.extendsFrom junit
}

sourceSets {
    main {
        java {
            srcDir 'java'
        }
    }
    test {
        java {
            srcDir 'test'
        }
    }
}

dependencies {
    compile project(':Container'),
            project(':engine-chunk'),
            project(':engine-api')

    runtime project(':engine-tuplesource')
}

spotless {
    java {
        targetExclude(
        )
    }
}
