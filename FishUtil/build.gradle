plugins {
    id 'java-library'
}

dependencies {
    api project(':Base'), project(':DataStructures'), project(':IO'), project(':Configuration'), project(':log-factory')
    api depCommonsEmail, depCommonsNet

    testCompile project(path: ':Base', configuration: 'tests')
}