plugins {
    id 'io.deephaven.project.register'
}

configurations {
    implementation.extendsFrom fishConfig
}

dependencies {
    compileOnly libs.autoservice
    annotationProcessor libs.autoservice.compiler
}
