plugins {
    id 'java-library'
    id 'io.deephaven.project.register'
}

dependencies {
    api libs.picocli
    api project(':java-client-session')

    api platform(libs.grpc.bom)
    api libs.grpc.api
}
