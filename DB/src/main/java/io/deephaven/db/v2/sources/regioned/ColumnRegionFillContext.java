package io.deephaven.db.v2.sources.regioned;

import io.deephaven.db.v2.sources.chunk.ChunkSource;

public class ColumnRegionFillContext implements ChunkSource.FillContext {

    /*
     * While there aren't currently any "special" features in this implementation, it serves as a useful placeholder
     * in case we need to re-add any kind of resource caching or release.
     */
}
